<UserControl
    x:Class="WpfApp6.View.Draw"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:WpfApp6.View"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:WpfApp6.Model"
    Width="1150"
    d:DataContext="{d:DesignInstance model:MODEL_ViewModel}"
    d:DesignHeight="450"
    Background="Red"
    mc:Ignorable="d">



    <Grid Margin="0,0,10,60" Background="AliceBlue" >


        <ItemsControl
            Width="1140"
            HorizontalAlignment="Left"
            
            Background="DarkBlue"
            ItemsSource="{Binding MODEL_Contest_FREQUENCIES}" Margin="0,0,0,-40">
            <ItemsControl.ItemTemplate>
                <DataTemplate>



                    <ItemsControl
                        Width="600"
                        Height="100"
                        Margin="5"
                        HorizontalAlignment="Stretch"
                        Background="Cyan"
                        ItemsSource="{Binding MODEL_Contest_AGECATEGORIES}">

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Grid
                                    Width="50"
                                    Height="50"
                                    Margin="5"
                                    Background="Magenta" />

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>


                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>


    </Grid>
</UserControl>
